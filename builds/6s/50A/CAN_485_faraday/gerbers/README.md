# gerbers/ — 6S / 50A / CAN-FD+RS-485 / Faraday

Empty. `../kicad/open_secure_esc_6s_50a_can485_faraday.kicad_pcb` now exists
with footprints placed and a board outline (see `../kicad/README.md`), but it
has no netlist imported and is not routed or DRC-clean yet — gerbers are
generated by exporting fabrication output (`Plot` in KiCad's PCB Editor, or
`kicad-cli pcb export gerbers` / `... export drill`) only after the board is
netlisted, routed, the BOM in `../README.md` is locked from `Candidate` to
settled per `AGENTS.md` §5, and the board passes DRC.

When populated, this folder should hold the standard fab set (`*.gbr` per
copper/mask/silk/edge layer, `*.drl` drill files, and a `*.gbrjob` job file)
plus a `README.md` update noting the KiCad version and export settings used,
per `AGENTS.md` §4 (record *why*, not just *what*).

Tracked in `TODO.md` 10.2 (BOM finalization) → 10.3 (manufacturing/fab
release).
